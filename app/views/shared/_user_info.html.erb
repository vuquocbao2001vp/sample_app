<%= link_to gravatar_for(current_user, size: Settings.gravar_user_size_50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("shared.view_profile_text"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t("shared.micropost_text") %></span>
